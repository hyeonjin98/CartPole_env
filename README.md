You should download Eigen library and yaml-cpp library before.

in the cfg.yaml file you can change any parameters of CartPole.
after build c++ codes, you can import in python as "import cart_pole_module"
